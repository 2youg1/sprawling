# JOB.md — <one-line task name>

> The task for this session. The agent **reads it in full** and leaves it unchanged.
> Whoever assigns the work writes it. It lands on disk before the `run_started` event: the task file exists first, then the Run exists.

## Task

(One line. What to produce.)

## Goal

(What counts as success, what counts as failure, and when to stop. **Write all three**: a goal that states only the success condition leaves the agent working past it.)

## Budget

(Ceilings for money and tokens. A missing budget hands the ceiling to chance.)

## Background

(What this work needs and the code does not say.)

## Delivery

(Where it goes, in what format, and who verifies it.)
